# Vite / Electron — copy to .env or .env.local for dev; .env.production for release

# Sentry (renderer + main). Production: set DSN only. Dev smoke test: also set ENABLE_DEV.
# VITE_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
# VITE_SENTRY_ENABLE_DEV=true